CREATE TABLE SWITCH_MANAGEMENT (
    ID INT GENERATED BY DEFAULT AS IDENTITY,
    NAME VARCHAR2(20) UNIQUE,
    STATE NUMBER(1) NOT NULL,
    PRIMARY KEY(ID)
);

INSERT INTO SWITCH_MANAGEMENT (
    NAME, STATE
) VALUES (
    'APP', 1
);

CREATE TABLE WEEKLY_HOURS (
    ID NUMBER(6) PRIMARY KEY,
    MONDAY NUMBER(4, 2) DEFAULT 0,
    TUESDAY NUMBER(4, 2) DEFAULT 0,
    WEDNESDAY NUMBER(4, 2) DEFAULT 0,
    THURSDAY NUMBER(4, 2) DEFAULT 0,
    FRIDAY NUMBER(4, 2) DEFAULT 0
);

INSERT INTO WEEKLY_HOURS (ID) VALUES (202411);

CREATE TABLE DAILY_HOURS (
    ID INT GENERATED BY DEFAULT AS IDENTITY,
    ACTION VARCHAR2(20) NOT NULL,
    EPOCH NUMBER(13),
    RECORDED_ON DATE DEFAULT TRUNC(SYSDATE),
    PRIMARY KEY(ID)
);

CREATE TABLE WEEK_PROGRAM (
    ID NUMBER(6) PRIMARY KEY,
    MONDAY NUMBER(1),
    TUESDAY NUMBER(1),
    WEDNESDAY NUMBER(1),
    THURSDAY NUMBER(1),
    FRIDAY NUMBER(1)
);

COMMIT;
