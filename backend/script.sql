CREATE TABLE SWITCH_MANAGEMENT (
    ID INT GENERATED BY DEFAULT AS IDENTITY,
    NAME VARCHAR2(20) UNIQUE,
    STATE NUMBER(1) NOT NULL,
    PRIMARY KEY(ID)
);

INSERT INTO SWITCH_MANAGEMENT (
    NAME, STATE
) VALUES (
    'APP', 1
);

CREATE TABLE HALTS (
    ID INT GENERATED BY DEFAULT AS IDENTITY,
    IN_REFERENCE VARCHAR2(30) NOT NULL,
    CURRENT_VALUE INT NOT NULL,
    PRIMARY KEY(ID)
);

INSERT INTO HALTS (
    IN_REFERENCE, CURRENT_VALUE
) VALUES (
    'COMMON', 7000
);

CREATE TABLE WEEKLY_HOURS (
    ID NUMBER(6) PRIMARY KEY,
    MONDAY NUMBER(4, 2) DEFAULT 0,
    TUESDAY NUMBER(4, 2) DEFAULT 0,
    WEDNESDAY NUMBER(4, 2) DEFAULT 0,
    THURSDAY NUMBER(4, 2) DEFAULT 0,
    FRIDAY NUMBER(4, 2) DEFAULT 0
);

INSERT INTO WEEKLY_HOURS (ID) VALUES (202411);

CREATE TABLE DAILY_HOURS (
    ID INT GENERATED BY DEFAULT AS IDENTITY,
    ACTION VARCHAR2(20) NOT NULL,
    EPOCH NUMBER(13),
    RECORDED_ON DATE DEFAULT TRUNC(SYSDATE),
    PRIMARY KEY(ID)
);

CREATE TABLE WEEK_PROGRAM (
    ID NUMBER(6) PRIMARY KEY,
    MONDAY_MORNING_IN VARCHAR2(13),
    MONDAY_MORNING_OUT VARCHAR2(13),
    MONDAY_AFTERNOON_IN VARCHAR2(13),
    MONDAY_AFTERNOON_OUT VARCHAR2(13),
    TUESDAY_MORNING_IN VARCHAR2(13),
    TUESDAY_MORNING_OUT VARCHAR2(13),
    TUESDAY_AFTERNOON_IN VARCHAR2(13),
    TUESDAY_AFTERNOON_OUT VARCHAR2(13),
    WEDNESDAY_MORNING_IN VARCHAR2(13),
    WEDNESDAY_MORNING_OUT VARCHAR2(13),
    WEDNESDAY_AFTERNOON_IN VARCHAR2(13),
    WEDNESDAY_AFTERNOON_OUT VARCHAR2(13),
    THURSDAY_MORNING_IN VARCHAR2(13),
    THURSDAY_MORNING_OUT VARCHAR2(13),
    THURSDAY_AFTERNOON_IN VARCHAR2(13),
    THURSDAY_AFTERNOON_OUT VARCHAR2(13),
    FRIDAY_MORNING_IN VARCHAR2(13),
    FRIDAY_MORNING_OUT VARCHAR2(13),
    FRIDAY_AFTERNOON_IN VARCHAR2(13),
    FRIDAY_AFTERNOON_OUT VARCHAR2(13)
);

COMMIT;
